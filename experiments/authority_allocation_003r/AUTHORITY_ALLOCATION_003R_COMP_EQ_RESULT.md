# AUTHORITY-ALLOCATION-003r Computational-Equivalence Repair Result

## Status

`NO_REPLICATION_IN_INDEPENDENT_WITNESS`

## Scientific lineage

Original AA-003r scientific freeze:

`4f505a5ac33f5765ee4bcf2a85a989668f6ac4d8`

Serialization-repair freeze:

`15e505c23ae092d29c98adfbb98bf49d13fb2654`

Computational-equivalence repair freeze:

`078c2231bc9edf3a879264335f3b88357e30894d`

Frozen executable Git blob:

`f67145290d506b58532df90314510a95738569d7`

Before execution, the local executable bytes were verified with `git hash-object` to equal that exact frozen blob.

## Prospective equivalence gate

The frozen audit record established before execution:

`EQUIVALENCE_AUDIT_PASS`

with:

\[
f_{repaired}=f_{frozen},
\qquad
C_{repaired}=C_{frozen},
\qquad
T_{repaired}=T_{full}.
\]

No scientific object was changed by the computational repair.

## Execution

The frozen computational-equivalence executable was invoked exactly once with `--execute` and the frozen output path.

Boundary validation passed and the two histories remained exactly equal under the supplied quotient:

\[
O_b(H_1)=O_b(H_2).
\]

The future contract compared literally equal.

## Primary frozen observation

The preregistered primary comparison was:

\[
T_{rec}(H_1)\stackrel{?}{=}T_{rec}(H_2).
\]

Observed:

\[
\boxed{T_{rec}(H_1)=6}
\]

and

\[
\boxed{T_{rec}(H_2)=6}.
\]

Therefore:

\[
\boxed{T_{rec}(H_1)=T_{rec}(H_2).}
\]

The inherited frozen classification is:

`NO_REPLICATION_IN_INDEPENDENT_WITNESS`

## Result custody

Raw result JSON SHA-256:

`1bb3ee01973f943384652d7e64e316c736b8b5c2a77ef8c46da3dcf093a5c8b0`

Raw result JSON Git blob hash:

`b9dcdc35688d9c21230a01c5f46be44dc25af5df`

Raw result size:

`64075` bytes.

The output uses exact hexadecimal numerator/denominator audit serialization; no float path was used.

## Frozen scientific readout

This exact independent witness did **not** reproduce the AA-003a recovery-latency divergence under its prospectively frozen common future.

AA-003a remains a valid existence counterexample. This result does not establish global sufficiency of `(theta,L,U_pre)` and does not identify or exclude any generally correct finer quotient.

## Claim ceiling

This result establishes only non-replication in this exact deterministic independent witness under the inherited AA-003r scientific specimen.

It does not establish:

- that AA-003a was invalid;
- global sufficiency of `(theta,L,U_pre)`;
- that redundant parameterization is irrelevant to repairability generally;
- any correct finer repair-state quotient;
- any topology claim;
- any claim about released continuous XM;
- any OpenCore mechanism, implementation, or solution.
